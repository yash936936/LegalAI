services:
  legal-ai-agent:
    build: .
    ports:
      - "8501:8501"
    environment:
      - GOOGLE_API_KEY=${GOOGLE_API_KEY}
    volumes:
      - ./data:/app/data
      - ./chroma_db:/app/chroma_db
      - ./legal_agent.db:/app/legal_agent.db
    restart: unless-stopped